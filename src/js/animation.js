$('#to-header').on('click', function() {
    const images = $('#header').position().top;
    
    $('html, body'). animate(
    {
        scrollTop: images
    }, 
    900);
}) ;

$('#to-features').on('click', function() {
    const images = $('#features').position().top;
    
    $('html, body'). animate(
    {
        scrollTop: images
    }, 
    900);
}) ;

$('#to-video').on('click', function() {
    const images = $('#video').position().top;
    
    $('html, body'). animate(
    {
        scrollTop: images
    }, 
    900);
}) ;

$('#to-pricing').on('click', function() {
    const images = $('#pricing').position().top;
    
    $('html, body'). animate(
    {
        scrollTop: images
    }, 
    900);
}) ;

$('#to-team').on('click', function() {
    const images = $('#team').position().top;
    
    $('html, body'). animate(
    {
        scrollTop: images
    }, 
    900);
}) ;

$('#to-customers').on('click', function() {
    const images = $('#customers').position().top;
    
    $('html, body'). animate(
    {
        scrollTop: images
    }, 
    900);
}) ;

$('#to-contact').on('click', function() {
    const images = $('#contact').position().top;
    
    $('html, body'). animate(
    {
        scrollTop: images
    }, 
    900);
}) ;